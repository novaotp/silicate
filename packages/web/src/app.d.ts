declare global {
	namespace App {
		// interface Error {}
		interface Locals {
            jwt: string | undefined
        }
		// interface PageData {}
		// interface PageState {}
		// interface Platform {}
	}
}

export {};
